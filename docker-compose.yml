services:
  web:
    build: .
    ports:
      - "8080:8000"
    environment:
      - DJANGO_SECRET_KEY=change-me-in-production
      - DJANGO_DEBUG=false
      - DJANGO_ALLOWED_HOSTS=*
      - DB_DIR=/app/db
    volumes:
      - db_data:/app/db

volumes:
  db_data:
